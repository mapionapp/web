image: node:alpine

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - dist
    when: on_success

deploy:
  stage: deploy
  script:
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - master